using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace DesignPatternsTraining
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            CallDesignPattern.Singleton();

            CallDesignPattern.Factory();

            CallDesignPattern.Builder();

            CallDesignPattern.DependencyInjection();

            CallDesignPattern.AbstractFactory();

            CallDesignPattern.PoolObjectCall();

            CallDesignPattern.Prototype();

            CallDesignPattern.Adapter();

            CallDesignPattern.Bridge();

            CallDesignPattern.Composite();

            CallDesignPattern.Decorator();

            CallDesignPattern.FacadeCall();

            CallDesignPattern.DecoratorTwoOrPizzaMaker();

            CallDesignPattern.CompositeTwoOrMusicGenerator();

            CallDesignPattern.Flyweight();

            CallDesignPattern.Proxy();

            CallDesignPattern.ChainOfResponsibility();
            */

            //CallDesignPattern.CommandDesignPattern();
            //CallDesignPattern.Interpreter();
            CallDesignPattern.Iterator();

            Console.ReadLine();
        }
    }
}
